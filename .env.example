# Supabase (https://supabase.com/dashboard)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Service role key (server-only, for admin API: list/update generated_lessons)
SUPABASE_SERVICE_ROLE_KEY=

# AI 文本生成：二选一或都配。优先 MiniMax（课程生成用 M2.5，提取/题目用 M2.1-lightning）
MINIMAX_API_KEY=
# 国内版 minimaxi.com：MINIMAX_BASE_URL 必填。若仍 invalid api key，可加 MINIMAX_USE_OPENAI=1 且改为 https://api.minimaxi.com/v1
MINIMAX_BASE_URL=
# 国内版若要求传 group_id，在「账户管理-基本信息」获取后填这里
MINIMAX_GROUP_ID=
# 国内版走 OpenAI 兼容接口时设为 1
# MINIMAX_USE_OPENAI=1
# OpenAI（可选；未配 MINIMAX 时用于课程/概念/题目生成；上传资料中的图片解析目前仅支持 OpenAI）
OPENAI_API_KEY=

# GitHub (optional, for daily cron trending repos)
GITHUB_TOKEN=

# Cron (optional, protect /api/cron/daily-course-generation 与 /api/cron/generate-path-lessons)
CRON_SECRET=

# App URL (for auth redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Admin (comma-separated emails allowed to access /admin and PATCH lessons)
ADMIN_EMAILS=
